TEST DRIVEN
-----------
1. Stub out all logincontroller tests and skip those that cannot pass

2. Request validator
   Throw errors of a known type
   Catch in middleware and return
   Check error codes in tests
   Assert error codes in failure tests that do a POST

3. Real login start implementation and assert client ID in response URL
   Complete login start tests
   Cookie issuing and encryption
   All tests pass

4. Real login end
   Authorization server client

5. Wiremock to add

ADMIN
-----
1. Kotlin ID Token validation errors to include description

2. Node.js ID token validation, issuer check only if ISSUER environment variable is configured

3. Update Jira before Tuesday
